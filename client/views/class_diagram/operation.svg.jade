g.member(data-bind="svgcss: { static: isStatic }, translateY: posY")
	// ko template: { name: 'member-separator-tmpl', if: $index() !== 0 }
	// /ko

	text(data-bind="attr: { y: textPosY, x: textPosX }")
		tspan.visibility(data-bind="text: visibility")
		| &nbsp;
		tspan.name(data-bind="text: name")
		tspan.scobe (
		tspan.params(data-bind="text: paramsText")
		tspan.scobe )
		// ko if: type() !== 'void'
		| &nbsp;
		tspan.separator :
		| &nbsp;
		tspan.type(data-bind="text: type")
		// /ko

	// ko template: { name: 'member-underline-tmpl', if: isStatic }
	// /ko